function solve(A, B) {
  let index = -1;
  let l = 0;
  let r = A.length - 1;
  while (l <= r) {
    mid = Math.floor((r + l) / 2);
    if (mid == A[0]) {
    }
  }
  return index;
}

A = [4, 5, 6, 7, 0, 1, 2, 3];
B = 4;
console.log(solve(A, B));
